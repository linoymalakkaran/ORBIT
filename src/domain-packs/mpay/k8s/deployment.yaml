apiVersion: apps/v1
kind: Deployment
metadata:
  name: mpay-domain-pack
  namespace: ai-portal
  labels:
    app: mpay-domain-pack
    domain: mpay
    compliance: pci-dss
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mpay-domain-pack
  template:
    metadata:
      labels:
        app: mpay-domain-pack
        domain: mpay
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "mpay-domain-pack"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/mpay/config"
    spec:
      serviceAccountName: mpay-domain-pack
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 2000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: mpay-domain-pack
          image: harbor.ai.adports.ae/orbit/mpay-domain-pack:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: DOMAIN_NAME
              value: mpay
            - name: DATA_CLASSIFICATION
              value: restricted
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
---
apiVersion: v1
kind: Service
metadata:
  name: mpay-domain-pack
  namespace: ai-portal
spec:
  selector:
    app: mpay-domain-pack
  ports:
    - port: 80
      targetPort: http
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mpay-domain-pack
  namespace: ai-portal
  annotations:
    vault.hashicorp.com/role: "mpay-domain-pack"
