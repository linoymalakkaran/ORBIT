apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkmarx-mcp
  namespace: ai-portal
  labels:
    app: checkmarx-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: checkmarx-mcp
  template:
    metadata:
      labels:
        app: checkmarx-mcp
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "checkmarx-mcp"
        vault.hashicorp.com/agent-inject-secret-env: "secret/data/orbit/checkmarx-mcp/config"
        vault.hashicorp.com/agent-inject-template-env: |
          {{- with secret "secret/data/orbit/checkmarx-mcp/config" -}}
          export CHECKMARX_URL="{{ .Data.data.checkmarx_url }}"
          export CHECKMARX_CLIENT_ID="{{ .Data.data.client_id }}"
          export CHECKMARX_CLIENT_SECRET="{{ .Data.data.client_secret }}"
          {{- end }}
    spec:
      serviceAccountName: checkmarx-mcp
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: checkmarx-mcp
          image: harbor.ai.adports.ae/orbit/checkmarx-mcp:latest
          imagePullPolicy: Always
          command: ["/bin/sh", "-c"]
          args: ["source /vault/secrets/env && uvicorn main:app --host 0.0.0.0 --port 8080"]
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: checkmarx-mcp
  namespace: ai-portal
spec:
  selector:
    app: checkmarx-mcp
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: checkmarx-mcp
  namespace: ai-portal
