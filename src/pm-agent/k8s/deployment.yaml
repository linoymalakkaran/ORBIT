apiVersion: apps/v1
kind: Deployment
metadata:
  name: pm-agent
  namespace: ai-portal
  labels: { app: pm-agent }
spec:
  replicas: 1
  selector:
    matchLabels: { app: pm-agent }
  template:
    metadata:
      labels: { app: pm-agent }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "8000"
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "pm-agent"
        vault.hashicorp.com/agent-inject-secret-env: "secret/data/pm-agent/env"
        vault.hashicorp.com/agent-inject-template-env: |
          {{- with secret "secret/data/pm-agent/env" -}}
          export PM_LITELLM_API_KEY="{{ .Data.data.litellm_api_key }}"
          {{- end }}
    spec:
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      serviceAccountName: pm-agent
      containers:
        - name: pm-agent
          image: harbor.ai.adports.ae/orbit/pm-agent:latest
          imagePullPolicy: Always
          command: ["/bin/sh", "-c"]
          args: ["source /vault/secrets/env && exec uvicorn app.main:app --host 0.0.0.0 --port 8000"]
          ports: [{ containerPort: 8000 }]
          env:
            - name: PM_LITELLM_API_BASE
              value: "http://litellm-gateway.litellm.svc:4000"
            - name: PM_DEFAULT_MODEL
              value: "gpt-4o-mini"
            - name: PM_JIRA_MCP_URL
              value: "http://jira-mcp.ai-portal.svc:80"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.monitoring.svc:4317"
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          securityContext: { allowPrivilegeEscalation: false }
          livenessProbe:
            httpGet: { path: /health/live, port: 8000 }
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet: { path: /health/live, port: 8000 }
            initialDelaySeconds: 10
            periodSeconds: 10
      imagePullSecrets: [{ name: harbor-pull-secret }]
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pm-agent
  namespace: ai-portal
---
apiVersion: v1
kind: Service
metadata:
  name: pm-agent
  namespace: ai-portal
spec:
  selector: { app: pm-agent }
  ports: [{ port: 80, targetPort: 8000 }]
